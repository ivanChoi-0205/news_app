import 'package:flutter/material.dart';

class BloggerLayout extends StatelessWidget {
  const BloggerLayout({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'blogger layout',
      ),
    );
  }
}
